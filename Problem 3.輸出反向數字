#include <stdio.h>
#include <stdlib.h>

int main() {
    int num;

    scanf("%d", &num);
    
    printf("%d,%d,%d,%d\n", num % 10, (num / 10) % 10, (num / 100) % 10, (num / 1000));
    
    return 0;
}
