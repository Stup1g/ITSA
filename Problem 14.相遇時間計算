#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int m;
    cin >> m;

    double time = m / 0.238;

    int result = (int)ceil(time);

    cout << result << endl;

    return 0;
}
