#include <iostream>  // 使用 cin/cout
using namespace std;

int main() {
    int base, height;
    double area;

    cin >> base >> height;

    area = (base * height) / 2.0;

    cout << "Triangle area:" << fixed;
    cout.precision(1); // 設定小數點一位
    cout << area << endl;

    return 0;
}
