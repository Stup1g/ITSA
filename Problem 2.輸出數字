#include <stdio.h>
#include <stdlib.h>

int main() {
    int num, a, b, c, d;

    scanf("%d", &num);

    a = num / 1000;
    b = num % 1000 / 100;
    c = num % 100 / 10;
    d = num % 10;

    printf("%d\n%d\n%d\n%d\n", a, b, c, d);

	return 0;
}
