#include <stdio.h>

int main() {
    int m, n;

    scanf("%d %d", &m, &n);

    while (n != 0) {
        int temp = n;
        n = m % n;
        m = temp;
    }

    printf("%d\n", m);

    return 0;
}
