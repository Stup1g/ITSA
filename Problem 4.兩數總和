#include <stdio.h>
#include <stdlib.h>

int main() {
    int n, a, b, c;
    scanf("%d", &n);

    for (int i = 0; i < n; i++) {
        scanf("%d %d", &a, &b);

        c = a + b;
        printf("%d\n", c);
    }

	return 0;
}
