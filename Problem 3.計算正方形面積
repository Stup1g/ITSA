#include <stdio.h>
#include <stdlib.h>

int main() {
    int n;
    double area, length;

    scanf("%d", &n);

    for (int i = 0; i < n; i++) {
        scanf("%lf", &length);

        area = round(length * length * 10) / 10;

        printf("%.1f\n", area);
    }

	return 0;
}
