#include <stdio.h>
#include <stdlib.h>

int main() {
    int num;

    scanf("%d", &num);

    if (num > 0) {
        printf("正數\n");
    }else if (num == 0) {
        printf("0\n");
    }else {
        printf("負數\n");
    }

	return 0;
}
