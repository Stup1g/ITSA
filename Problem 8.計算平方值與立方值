#include <stdio.h>
#include <stdlib.h>

int main() {
    int n, a, b, c;

    scanf("%d", &n);

    for (int i = 0; i < n; i++) {
        scanf("%d", &a);

        b = a * a;
        c = b * a;

        printf("%d %d %d\n", a, b, c);
    }
	return 0;
}
