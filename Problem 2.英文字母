#include <iostream>
#include <cctype>

using namespace std;

int main() {
    char letter;
    cin >> letter;

    letter = toupper(letter);

    if (letter == 'A' || letter == 'E' || letter == 'I' || letter == 'O' || letter == 'U') {
        cout << "母音" << endl;
    } else {
        cout << "子音" << endl;
    }

    return 0;
}
