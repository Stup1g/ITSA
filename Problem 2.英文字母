#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

int main() {
    char letter;

    scanf("%c", &letter);

    letter = toupper(letter);

    if (letter == 'A' || letter == 'E' || letter == 'I' || letter == 'O' || letter == 'U') {
        printf("母音\n");
    } else {
        printf("子音\n");
    }

    return 0;
}
