#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    int upper, lower, height;
    double area;

    cin >> upper >> lower >> height;

    area = ((upper + lower) * height) / 2.0;

    cout << "Trapezoid area:" << fixed << setprecision(1) << area << endl;

    return 0;
}
