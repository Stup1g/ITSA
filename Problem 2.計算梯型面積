#include <stdio.h>

int main() {
    int upper, lower, height;
    double area;

    // 讀取底和高
    scanf("%d %d %d", &upper, &lower, &height);

    // 計算三角形面積
    area = ((upper + lower) * height) / 2.0;

    // 輸出結果
    printf("Trapezoid area:%.1f\n", area);

    return 0;
}
