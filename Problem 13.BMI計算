#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    int w, h;
    double bmi, hmeters;

    cin >> w >> h;

    hmeters = h / 100.0;
    bmi = w / (hmeters * hmeters);

    cout << fixed << setprecision(2) << bmi << endl;
    return 0;
}
