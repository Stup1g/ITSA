#include <stdio.h>
#include <stdlib.h>

int main() {
    int num;

    scanf("%d", &num);

    for (int i = 7; i >= 0; i--) {
        printf("%d", (num >> i) & 1);
    }
    printf("\n");

    return 0;
}
